import {vec2} from 'gl-matrix'


export class R2 {
    static X = vec2.fromValues(1.0, 0.0)
    static Y = vec2.fromValues(0.0, 1.0)

    static ZERO = vec2.fromValues(0.0, 0.0)
    static ONE = vec2.fromValues(1.0, 0.0)
}
